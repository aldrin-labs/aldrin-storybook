import { COLORS } from '@variables/variables'
import styled from 'styled-components'

export const TokenName = styled.span`
  font-weight: 600;
  color: ${COLORS.primaryWhite};
`

export const TokenNameWrap = styled.div`
  text-align: left;
  margin-left: 10px;
  margin-right: 10px;
`
