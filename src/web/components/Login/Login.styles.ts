import styled from 'styled-components'

export const SWrapperStyles = `
  z-index: 100000;
  padding: 0 1rem;
  align-items: center;
  display: flex;
  justify-content: flex-end;
`

export const SWrapper = styled.div`${SWrapperStyles}`
